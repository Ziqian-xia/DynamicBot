# nixpacks.toml — tells Railway how to build and run this project.
# The Python app lives in backend/, so we point nixpacks there explicitly.

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT"
