# nixpacks.toml — Railway build configuration.
# nixpacks auto-detects Python from root requirements.txt and runs pip install.
# We only override the start command to point into the backend/ subdirectory.

[start]
cmd = "cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT"
